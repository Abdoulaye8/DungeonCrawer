package attack;

/**
 * Implémentation concrète d'un équipement offensif de type "Sort".
 */
public class Sort extends OffensiveEquipment {
    public Sort(String name, int attackLevel) {
        super("Sort", name, attackLevel);
    }
}
